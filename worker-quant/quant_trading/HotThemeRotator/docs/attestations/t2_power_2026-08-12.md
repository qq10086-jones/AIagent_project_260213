# T2 power artifact attestation — 2026-08-12

Git-tracked witness for a machine-local artifact. `reports/` is
gitignored, so the artifact cannot be its own evidence: this file is
what ties those numbers to the implementation that produced them.
Derived from the stored artifact — **no simulation was re-run**.

## Identity

| item | value |
|---|---|
| artifact path (local) | `reports/research/t2_power/2026-08-12.json` |
| artifact sha256 | `c7a7e6cb3a33a66f0712654f3c05604a7232379a9d5315b9aba62deca4eb506e` |
| mapping sha256 (bucket_events actually used) | `ec9066dfffd7e8016785a4c133cd39d60f6511d98e0e09d629160b0a55eef64f` |
| mapping source | `t2_join_report_2026-08-10.json` (asof 2026-08-10) |
| events | H1 419 / H2 419, 159 shared |
| git revision at attestation | `aa101e0a086e74013c6af6c9d7631150000bc1c4` |

### Generator sources (line-ending-normalised sha256)

| file | sha256 | role |
|---|---|---|
| `tools/t2_power_artifact.py` | `547b8e6709f7cb6f49a447eba13c5b3d48374d9185264dfb543b8eff1d9aa33e` | runner: grid, scenario, size screen, reporting. **Edited AFTER the run** (added `--attest` and corrected the size wording); the numeric configuration it used is recorded independently under Run parameters, written by the run itself |
| `src/hot_theme_rotator/research/full_model_power.py` | `d543c5aeb327162ce5a810b219e5d3574b9934e30c9b0f24870ce3ab16f9ed87` | **simulator + estimator — determines every number**; unchanged since the run |

## Run parameters

| item | value |
|---|---|
| seed | 20260812 |
| power draws per cell | 500 |
| size draws | 1000 |
| bootstrap replications | 199 |
| python | 3.13.0 |
| numpy | 2.3.5 |
| beta1 grid (spec P) | [0.0, 0.05, 0.1, 0.15, 0.2, 0.25, 0.3] |
| elapsed | 1156.5 s |

## Size screen — what it does and does not say

- observed FWER under the complete null: **0.05** over 1000 draws
- one-sided 95% Clopper-Pearson interval: [0.0392, 0.0629]
- pre-declared screen: **passed**

**The point estimate landing on alpha means the screen found nothing.**
It does NOT establish that the true size equals alpha — the interval
still reaches the upper bound above. Any wording along the lines of
"level verified" or "at nominal level" is an overclaim and was
corrected on 2026-08-12 after review.

> **Artifact corrected in place on 2026-08-12.** The size block gained an upper bound and lost the overclaiming verdict string; the
> original wording is retained inside the artifact under
> `size.superseded`. **No simulated number changed** — the bounds are arithmetic on the stored rejection count. A sha256 taken
> before that correction will therefore not match the one above.

## Headline power (read with the size screen above, not without it)

At the top of the pre-declared grid (beta1 = 0.3, drift 1.80% per 1 s.d. reaction):

- reject **either** hypothesis under Holm: **0.468**
- reject **both**: **0.216**

Across the whole grid `any` never reaches 0.5 and `both` never
reaches 0.25. The study is underpowered; that is a property of the
design, not of this run.

## Governance

- beta1*: **None** — not proposed here
- outcome read: **False**
- preregistration status: **DRAFT — NOT FROZEN, family NOT REGISTERED**
