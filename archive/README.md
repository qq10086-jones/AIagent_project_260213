# Archive Boundary

This directory is reserved for historical snapshots that must remain in the repository.

Target contents:

- dated backup directories
- archived workspace snapshots
- retired migration bundles

Rule:

- archived content should not stay at the repository root
- every archived snapshot should explain why it exists and when it can be removed
