{
  "_comment": "M8 Phase 1 STAGING cohort config. cohort_enabled=true only in staging. Mirrors production cohort definition but with runtime controls unlocked.",
  "version": "1.0-staging",
  "cohort_id": "m7_dynamic_routing_alpha_staging",
  "description": "M8 Phase 1 staging trial — fe_led workflow class only",
  "included_workflow_classes": ["fe_led"],
  "excluded_workflow_classes": ["high_risk_release_sensitive", "architectural_orchestration_required"],
  "allowed_project_types": ["web_app", "spa", "frontend_only"],
  "allowed_input_classes": ["ui_change", "styling", "component_refactor", "layout"],
  "rollback_trigger_thresholds": {
    "high_risk_misroute_pct": 2,
    "tier_misroute_pct": 5,
    "forced_sequential_spike_pct": 40,
    "circuit_breaker_trigger_pct": 10
  },
  "runtime_controls": {
    "cohort_enabled": true,
    "approved_by": "Architect — M8 Phase 1 staging trial authorization",
    "approval_date": "2026-03-09",
    "environment": "staging",
    "live_traffic": false
  }
}
