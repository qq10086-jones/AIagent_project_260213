# Quant Project Progress — 2026-03-11

## 1. 当前执行阶段 (Current Phase)
**Phase:** 生产级模型切换与实盘自动化升级（暨风控/新闻子系统重构启动）
**Status:** `worker-quant` 本地闭环能力已初步完善，正式从研究回测模式（Research）转向实盘预备模式（Paper Trading Ready），并启动了与 Nexus 框架集成相关的基础设施重构。

## 2. 本轮已完成工作 (What We Did)

### A. 模型主导权切换 (Model Pipeline Promotion)
- **动作**: 根据前期的 Learning M1 评估结果，将 `config.yaml` 和 `run_pipeline.py` 的主执行模型从表现较弱的 `ridge` 切换为最优的 **`shadow_ic`**，同时保留 `ridge` 和 `shadow_eq` 作为对照组平行运行（Compare Modes）。
- **结果**: 流水线端到端跑通。`shadow_ic` 验证回测展现了更高的最终净值（1.30M JPY，+30.07% 收益）和更稳健的回撤控制（-12.58%），正式确立为生产主模型。

### B. 实盘自动化与容错提升 (Operational Automation)
- **动作**: 修复了 `make_decision.py` 中关于组合最大回撤降仓逻辑的痛点。原先需要每日人工传入 `--peak_nav`，现已改为自动从 SQLite 数据库的 `account_snapshots` 表中查询历史最高净值（Peak NAV）。
- **结果**: 显著降低了运营的人工干预需求和实盘输入错误（Fat-finger）的风险。

### C. 评估体系闭环 (Evaluation Completeness)
- **动作**: 修复了此前在 `compute_ic.py` 中因缺失 `volume` 数据导致 `vol_z`（成交量确认信号）因子在 IC 评估中全为 0 的 Bug。
- **结果**: 目前全部 15 个 Alpha 因子均已实现真实交易宇宙（Screened Universe）下的 IC 计算与学习权重更新。

### D. News Worker 全量实施与质量加固 (News Worker P0-P4 + Bug Fix)

**阶段交付**（2026-03-11，详见 `news/design/NEWS_PROGRESS_2026-03-11.md`）

- **P0-P4 全部功能交付**：
  - `news_ingester.py`：Google RSS + TDnet RDF + J-Quants API 三源收集，SHA-256 去重，robots.txt 合规，指数退避重试，断流报警
  - `news_analyzer.py`：TF-IDF 事件聚类 + Claude LLM 打分，Token Bucket 限流，持仓优先
  - `eval_golden_set.py`：Golden Set 回归框架（100 条标注集，Macro-F1 ≥ 0.75 门控）
  - `data_archiver.py`：季度归档至 `*_archive` 表，原子事务，`data_lifecycle_log` 记录
  - `qa_pit_regression.py`：8/8 PIT 回归测试通过（Governance Rule 1.4）

- **QA 审视后 Bug 修复**（P0-P2 优化）：
  - 归档事务原子化；TIMEOUT 行孤儿问题修复
  - TDnet 代码匹配精确化（词边界正则）；em dash 编码问题修复
  - `sentiment_model_eval` 改为复合主键 `(model_version, eval_date)`，保留历史评估记录
  - `requirements.txt` 补全；新增 `validate_sources.py` 数据源实网验证工具

- **当前测试状态**：`qa_pit_regression.py` 8/8 PASS；所有 smoke test 通过

---

## 3. 当前架构状态评估 (Architecture State)

目前 `worker-quant` 作为一个**单体量化管线**，其本地内聚度极高，闭环能力完整。
数据更新 (`db_update`) -> 选股过滤 (`screener`) -> Alpha打分与回测 (`ss7`) -> 订单打包与风控 (`make_decision`) -> 因子效果自我学习 (`compute_ic`) 已经可以完全跑通。

但作为 Nexus 系统的一个 **Worker** 节点，其工程解耦和接口契约仍未完全达标，尤其是 News 模块仍处于严重的耦合状态。

---

## 4. 遗留问题与下一步 (Next Steps)

### 近期（生产准备）

- **验证 TDnet/J-Quants**（需网络）：`python news/validate_sources.py`，确认解析逻辑与实际响应匹配
- **首次真实运行**：`news_ingester.py → news_analyzer.py`，确认数据入库正常
- **扩充 Golden Set 至 ≥200 条**（人工标注），达到 Macro-F1 统计可信阈值

### 中期（质量提升）

- **差异化衰减**：ss7 消费端改用 `expected_impact_days` 替代全局固定半衰期（需与 quant 模型迭代协调）
- **归档调度配置**：设置季度 cron，每季度第一个工作日执行 `data_archiver.py`

### 长期（扩展）

- **Nexus 接入**：等 News Worker 稳定后，完成 Worker Contract 对齐，接入 Nexus 调度体系
- **适配 Nexus Orchestrator 接口**：将 `make_decision.py` 的 `decision_snapshot.json` 对齐到 Nexus 标准 `quant_execution` 契约（等待 Orchestrator 框架稳定后执行）

---

## 5. 项目元数据 (Meta)
- **主导组件**: `ss7_sqlite_news_overlay.py`, `run_pipeline.py`, `compute_ic.py`
- **最新回测基准**: `shadow_ic` 模式 (Win: +30.07%, Max DD: -12.58%)
- **最后更新时间**: 2026-03-11