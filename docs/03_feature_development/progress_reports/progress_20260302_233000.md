# 项目进展报告 - 2026-03-02（夜间更新）

## 核心进展
1. 已完成 `news.tdnet_close_flash` 报告链路修复：
- Discord 支持结构化分点输出（重点结论/风险温度/原文链接）。
- 已恢复 HTML 报告产物并可作为附件输出。
- `/run <tool_name>` 显式指令入口已加到 Orchestrator，避免命令被语义路由吞掉。
2. 已完成环境修复与重启：
- `orchestrator`、`brain`、`worker-quant` 重建并启动成功。
- 修复了 DashScope 兼容端点配置：`QWEN_BASE_URL` -> `https://dashscope-intl.aliyuncs.com/compatible-mode/v1`。

## 已记录问题（新增）
1. 用户输入：`40W日元本金，明天怎么操作？目前空仓`  
现象：返回 `Error: Qwen API error 404 Not Found`。  
初步判断：路由/调用链仍存在部分路径使用错误 Qwen 端点，或部分服务未加载最新 env。  
状态：已记录，待专项排查（见待完成事项 A-1）。

## 当前状态评估
- Coder 主链路：可用（含审批门控）。
- Quant 新闻报告链路：可用，质量已提升到“可读+可追溯链接”。
- 新闻原文直链：在 Google RSS 样本上不稳定，已提供“原文检索直达链接”兜底。

## 待完成事项（本轮新增）
### A. 紧急问题修复
1. 排查并修复 `40W日元本金...` 场景的 Qwen 404（统一所有服务端点与模型配置加载）。

### B. 新闻体系剩余两步（待完成）
1. 给 `source_links` 增加“媒体主页/频道页”备份链接（当原文直链不可得时，至少可一跳到媒体站内）。
2. 在报告中增加“来源可信度标签”（官方媒体/聚合/论坛）与简单分级展示。

### C. 项目级待完成事项（汇总）
1. 继续清理 `worker-quant/worker.py` 历史冗余逻辑，降低回归风险。
2. 统一新闻工具输出契约（统一分点、链接字段、artifacts结构）。
3. 完成 canary 回归包（覆盖 `/coder`、`/run news.tdnet_close_flash`、审批流、失败回退）。
4. Dashboard 增强：展示 `tasks.result_json` 的关键字段（status/facts/source_links/artifacts）。

---
**Reported by:** Codex  
**Timestamp:** 2026-03-02  
**Status:** 主链路可用，存在配置一致性与新闻源稳定性待收敛。

## 深夜续推进（2026-03-02 18:40 JST）
1. 已完成本轮代码改造并重启服务：
- `orchestrator/src/index.js`：
  - Qwen 端点重试与 fallback（`base` / `compatible-mode/v1` / `v1`）。
  - “本金+空仓+次日操作”语句强制路由到 `quant.discovery_workflow`。
  - discovery 模式关闭 brain 重试，避免重复入队。
  - 对该类语句在调用 brain 前强制注入：`quick_mode=true, time_budget_s=75, max_attempts=2, min_candidates=2`。
- `worker-quant/worker.py`：
  - discovery 增加时间预算与 quick 模式（早停、候选目标收敛、运行时长回传）。
  - LLM 调用超时参数化（`LLM_TIMEOUT_S`），并降低 news-risk 评分调用超时。
- `brain/supervisor.py`：
  - discovery payload 白名单加入 quick 参数。
  - 资金规划类 discovery 快速通道：跳过 `news.daily_report`，并在筛选阶段默认补全 quick 参数。
2. 已完成回归验证：
- `Qwen API error 404` 不再直接对外暴露（已进入端点自适应+回退路径）。
- 最新任务 payload 已验证包含 quick 参数：
  - `{"quick_mode":true,"time_budget_s":75,"max_attempts":2,"min_candidates":2}`。
- `worker-quant` 日志已验证生效：`Quick=True Attempts=2 BudgetS=75`。
3. 当前未完全关闭的问题（待完成）：
- `/chat` 在部分调用中仍会返回通用 `unknown Analysis Report`，与后端任务实际执行结果存在时序/聚合不一致。
- 历史 run 里仍有 `news.daily_report` 长时间 `running` 的旧任务残留，需要单独清理与超时治理。

## 待完成（下一步）
1. 修复 Brain 最终叙事聚合：优先使用当前 `run_id` 的 `discovery_result`，禁止回退到通用 unknown 模板。
2. 给 `news.daily_report` 增加硬超时与降级返回，避免阻塞 discovery 链路。
3. 增加 `/chat` 同步等待上限与“异步已受理”响应模式（超过阈值返回 run_id + 进度，而非超时/空泛文本）。
